var changed = 0

function change(num)
{
    return (num + 5) / 3
}

changed = change(16)

var processed = 0

function processArg(num)
{
    return (num + 3) / 5
}

processedArg = processArg(17)

console.log(changed)
console.log(processedArg)
